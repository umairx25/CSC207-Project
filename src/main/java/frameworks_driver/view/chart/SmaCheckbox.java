package frameworks_driver.view.chart;

import javax.swing.*;

/**
 * Represents a checkbox for selecting the Simple Moving Average (SMA) option in the chart view.
 */
public class SmaCheckbox extends JCheckBox {

    /**
     * Constructs a new SmaCheckbox with a predefined label.
     */
    public SmaCheckbox() {
        super("Simple Moving Average (SMA)");
    }
}
